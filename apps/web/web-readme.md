# Web Module

**Purpose:** Pure React (Vite) Single Page Application frontend.

**Note:** This UI layer will be built last. It handles the user chat interface, houses the tldraw 2D sketching canvas, and renders the output 3D geometry files.
