# Parameter Agent

**Purpose:** Simple Path Constraint Checker.

**Note:** Interacts dynamically with the user to gather parameters if required variables (like width, radius, height) are missing for whitelisted primitives. Outputs explicit modeling steps.
